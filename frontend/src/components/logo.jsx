import React from "react";

const Logo = () => (
  <img 
    src="/images/logo.png" 
    alt="Rivera All Services"
    style={{
      maxHeight: "50px",
      width: "auto",
      display: "block"
    }}
  />
);

export default Logo; // ✅ Use only one export

